## Ejecución Docker
Ejecutar Docker, cd a root y en terminal "docker compose up"<br>
Acceso a jupyter notebook: http://127.0.0.1:3011  -> token: sparklab<br>

## Ejecución lstm
Instalacion:<br>
   python -m pip install --upgrade pip<br>
   python -m pip install -r requirements.txt<br>

## Ejecución rapidminer
Seleccionar la carpeta rapidminer como raiz del repositorio, en RapidMiner
